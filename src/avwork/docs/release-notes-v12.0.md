# Egret v12.0

This release delivers a unified outbound control plane with process-aware connection visibility, Little Snitch-style blocklist matching, rules, prompting, trust-context attachment, investigations, and enforcement audit hooks.

Native backend execution support is prepared but remains disabled by default. Enable only in controlled environments after validating platform-specific commands.
